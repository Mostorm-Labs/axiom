# perfect-freehand reference freeze

The reference is pinned to commit `176e00f2399f4969e1b0965c5921d96a3e50ce9f`.
The copied source under `verification/reference/perfect_freehand/` is attribution-only
and is verified against `.aegis/packages/GT-G4-5-R01/reference.lock.json` with
`git hash-object`. Expected StrokePoint and outline fixtures were generated by the
pinned upstream TypeScript implementation before the C++ evaluator was implemented.

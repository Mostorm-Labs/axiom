# GT-G4-08

G4-07 supplies local selection. Implement a renderer-neutral transient transform
override and a session that freezes selected targets at begin. Preview is local;
only finish may submit one existing `SetTransforms` payload.

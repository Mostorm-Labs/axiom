# GT-G4-12

The production path already exists under `runtime/input`, `runtime/ink`,
`runtime/interaction`, `runtime/render`, and `arc`. Add only an application
composition root and HUD. The host owns lifecycle and ingress; production
modules own brush/selection/eraser/canonical semantics. Web forwards batches;
Windows/Android entrypoints call native Arc InputSource. First action is the
RED composition test proving that no Ink Playground target exists at the
anchor. Physical device capture is a separate evidence class.

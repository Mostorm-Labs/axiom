add_library(axiom_brush_lab_common STATIC common/brush_lab.cpp common/reference_brushes.cpp)
target_include_directories(axiom_brush_lab_common PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/common)
target_link_libraries(axiom_brush_lab_common PUBLIC canvas_runtime_ink)
if(CANVAS_RENDER_ENABLE_SKIA_HEADLESS)
  target_link_libraries(axiom_brush_lab_common PUBLIC canvas_runtime_render)
endif()
target_compile_features(axiom_brush_lab_common PUBLIC cxx_std_20)
if(MSVC)
  target_compile_options(axiom_brush_lab_common PRIVATE /W4 /permissive- /WX /EHsc)
else()
  target_compile_options(axiom_brush_lab_common PRIVATE -Wall -Wextra -Wpedantic -Werror)
endif()

add_executable(axiom_brush_lab tools/brush_lab_main.cpp)
target_link_libraries(axiom_brush_lab PRIVATE axiom_brush_lab_common)
target_compile_features(axiom_brush_lab PRIVATE cxx_std_20)
if(MSVC)
  target_compile_options(axiom_brush_lab PRIVATE /W4 /permissive- /WX /EHsc)
else()
  target_compile_options(axiom_brush_lab PRIVATE -Wall -Wextra -Wpedantic -Werror)
endif()

if(BUILD_TESTING)
  add_executable(axiom_brush_lab_tests tests/brush_lab_test.cpp)
  target_link_libraries(axiom_brush_lab_tests PRIVATE axiom_brush_lab_common)
  target_compile_definitions(axiom_brush_lab_tests PRIVATE
    G45_GOLDEN_ROOT="${PROJECT_SOURCE_DIR}/verification/corpus/brush/g4_5/v1")
  target_compile_features(axiom_brush_lab_tests PRIVATE cxx_std_20)
  if(MSVC)
    target_compile_options(axiom_brush_lab_tests PRIVATE /W4 /permissive- /WX /EHsc /UNDEBUG)
  else()
    target_compile_options(axiom_brush_lab_tests PRIVATE -Wall -Wextra -Wpedantic -Werror)
  endif()
  add_test(NAME G45BrushLab COMMAND axiom_brush_lab_tests)
  add_executable(axiom_reference_brush_tests tests/reference_brush_test.cpp)
  target_link_libraries(axiom_reference_brush_tests PRIVATE axiom_brush_lab_common)
  target_compile_features(axiom_reference_brush_tests PRIVATE cxx_std_20)
  if(MSVC)
    target_compile_options(axiom_reference_brush_tests PRIVATE /W4 /permissive- /WX /EHsc /UNDEBUG)
  else()
    target_compile_options(axiom_reference_brush_tests PRIVATE -Wall -Wextra -Wpedantic -Werror)
  endif()
  add_test(NAME G45ReferenceBrushCompile COMMAND axiom_reference_brush_tests)
endif()
